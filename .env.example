# Compose 项目名（容器名前缀）
COMPOSE_PROJECT_NAME=relic-restore

# 前端映射端口（容器内固定 80）
FRONTEND_PORT=20110

# 后端映射端口（容器内固定 3000）
BACKEND_PORT=21110

# PostgreSQL 宿主机映射端口
DB_PORT=54320
DB_NAME=app_db
DB_USER=app_user
DB_PASSWORD=app_password

# JWT 签名密钥（生产环境请替换）
JWT_SECRET=local-dev-secret
